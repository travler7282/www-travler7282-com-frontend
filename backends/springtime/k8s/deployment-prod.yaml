apiVersion: apps/v1
kind: Deployment
metadata:
  name: springtime
  labels:
    app: springtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springtime
  template:
    metadata:
      labels:
        app: springtime
    spec:
      containers:
        - name: springtime
          image: docker.io/travler7282/springtime:0.0.1-SNAPSHOT
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: springtime
spec:
  selector:
    app: springtime
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springtime
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.travler7282.com
      secretName: springtime-tls
  rules:
    - host: api.travler7282.com
      http:
        paths:
          - path: /springtime/api/v1
            pathType: Prefix
            backend:
              service:
                name: springtime
                port:
                  number: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: springtime-tls
spec:
  secretName: springtime-tls
  dnsNames:
    - api.travler7282.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
