apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-devops-config-dev
data:
  config.yaml: |
    app:
      name: ai-devops-assistant
      max_log_size: 100000

    security:
      require_api_key: true

    rate_limit:
      enabled: true
      requests: 10
      window_seconds: 60

    rag:
      persist_dir: /data/chroma_db
      collection_name: logs

    retention:
      enabled: true
      max_age_seconds: 86400   # 24 hours
      cleanup_interval_seconds: 300  # every 5 minutes
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-assistant-dev
  labels:
    app: devops-assistant-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-assistant-dev
  template:
    metadata:
      labels:
        app: devops-assistant-dev
    spec:
      containers:
        - name: devops-assistant-dev
          image: docker.io/travler7282/devops-assistant:0.1.0-dev
          imagePullPolicy: Always
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ai-devops-secrets-dev
                  key: OPENAI_API_KEY
            - name: APP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ai-devops-secrets-dev
                  key: APP_API_KEY
            - name: APP_CONFIG
              value: /config/config.yaml
          volumeMounts:
            - name: config-dev
              mountPath: /config
            - name: data-dev
              mountPath: /data
          ports:
            - containerPort: 8000
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
      volumes:
        - name: config-dev
          configMap:
            name: ai-devops-config-dev
        - name: data-dev
          persistentVolumeClaim:
            claimName: chroma-pvc-dev
---
apiVersion: v1
kind: Service
metadata:
  name: devops-assistant-dev
spec:
  selector:
    app: devops-assistant-dev
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-assistant-dev
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - dev-api.travler7282.com
      secretName: devops-assistant-dev-tls
  rules:
    - host: dev-api.travler7282.com
      http:
        paths:
          - path: /devops-assistant/api/v1
            pathType: Prefix
            backend:
              service:
                name: devops-assistant-dev
                port:
                  number: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: devops-assistant-dev-tls
spec:
  secretName: devops-assistant-dev-tls
  dnsNames:
    - dev-api.travler7282.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
