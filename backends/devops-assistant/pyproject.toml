[project]
name = "devops-assistant"
version = "0.1.0"
description = "DevOps AI Assistant backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --cov=. --cov-report=term-missing"
