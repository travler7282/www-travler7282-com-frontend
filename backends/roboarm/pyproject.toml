[project]
name = "roboarm"
version = "0.1.0"
description = "RoboArm BLE controller backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --cov=. --cov-report=term-missing"
